Your task is to convert narrative input into timed 5-15 second multimedia segments(Strictly each segment should have its own independent meaning,so that if  segment is missing it should not effect the whole video)..Most important and strictly All dialogues should be carry meaning together as well as alone. For each segment provide:
    
   
     
    Visual description: Manim-compatible scene description (geometric primitives, mobjects, animations)(No constraints on visual description use whatever suits fine for the story)

    Speech text:  Strictly contains words to be spoken only in lower case latin letters words and "," or "." or "?",(if you want to use numbers or mathematical operation use words only example five plus two equals seven,use phi or theta etc for their symbols etc ),strictly no symbols or special characters

    Format strictly as plain-text JSON:
    {"segment1": ["visual description", "speech text"], "segment2": ["...", "..."], ...}

    Requirements:

    Use ONLY segment1, segment2 sequential numbering

    Visuals must use explicit Manim elements:
    ✓ Parametric shapes (Polygon, Arrow, Dot)
    ✓ Mathematical surfaces (Surface, 3DAxes)
    ✓ Color codes (HEX/RGB)
    ✓ Transform animations (Create, FadeIn, Rotate)
    ✓ No mention about commands or actions just the text that is to be spoken
    ✓ No mention of the audio or the flow
    Striclty do not add any code in visuals or audio(you can ignore that part of story)

    Speech text requirements:
    ✓ No contractions ("do not" not "don't")
    ✓ Max 1 comma per sentence
    ✓ Oxford comma usage
    ✓ Terminal punctuation only (.?!)
    ✓ No mention about commands or actions just the text that is to be spoken
    ✓ No mention of the visuals or the flow

    JSON must be:
    ✓ Wrapped in {} with double-quoted strings
    ✓ Proper comma placement
    ✓ No trailing commas
    ✓ Escape characters as " if needed

    Example:
    {"segment1": ["A blue RegularPolygon(5) rotates clockwise on an orange background", "The ancient symbol begins to glow with pentagonal symmetry."]}

    Input Story:
    {Okay, here is a script breakdown for a very short YouTube video (~60 seconds) about Python's `rich` module.

---

**Video Title:** Python `rich`: Make Your Terminal BEAUTIFUL! ✨ (In 60 Seconds)

**Video Description:**
Tired of boring terminal output? Learn how the Python `rich` library can instantly add color, tables, progress bars, and more to your command-line apps! #python #rich #terminal #coding #programming #shorts

**(Video Structure: Use fast cuts, clear visuals of code AND terminal output side-by-side or alternating quickly)**

---

**SCRIPT:**

**(0-5 seconds) VISUAL:** Start immediately with a split screen. Left: Plain, boring `print("Hello", {"data": [1, 2, 3], "status": "OK"})` output in a terminal. Right: The *same data* printed using `rich.print`, showing colors and nice formatting. Upbeat, quick background music starts.
**AUDIO (Voiceover, energetic):** "Is your Python terminal output looking... blah?"

**(5-15 seconds) VISUAL:** Transition to full screen showing the `rich` output from the previous scene. Then, quickly show the code: `from rich import print` followed by the `print(...)` call. Highlight the `from rich import print` line.
**AUDIO:** "Meet `rich`! A Python library to make your terminal output beautiful and informative, with almost NO extra effort."

**(15-25 seconds) VISUAL:** Show code: `print("This is [bold red]important![/bold red]")`. Show the resulting terminal output with "important!" in bold red. Then quickly show `print({"user": "Alice", "id": 123, "active": True})` and its pretty-printed, colored output.
**AUDIO:** "`rich` upgrades `print`. It adds color, styles using simple markup, and automatically pretty-prints dictionaries, lists, and more!"

**(25-40 seconds) VISUAL:** Show code snippet for creating a simple `rich.table.Table`. Add 2-3 rows quickly. Show the clean, formatted table rendered in the terminal.
**AUDIO:** "Need tables? `rich` makes creating clean, readable tables super easy!"

**(40-50 seconds) VISUAL:** Show a simple loop using `from rich.progress import track` e.g., `for i in track(range(10), description="Processing..."): time.sleep(0.1)`. Show the animated progress bar running in the terminal.
**AUDIO:** "It even does awesome progress bars with just one line!"

**(50-55 seconds) VISUAL:** Show the command `pip install rich` clearly on screen.
**AUDIO:** "Get started in seconds: `pip install rich`."

**(55-60 seconds) VISUAL:** Quick montage flash of the cool outputs (colored text, table, progress bar). End screen with text: "Explore Python Rich!" and maybe your channel logo/subscribe button. Music swells slightly and fades out.
**AUDIO:** "`rich` does SO much more! Better tracebacks, syntax highlighting... Go check it out and level up your CLI apps! Happy coding!"

---

**Production Notes:**

*   **Pacing:** Keep it FAST. Each segment is only a few seconds.
*   **Visuals:** Clear code, clear terminal output. Side-by-side comparisons are effective. Use highlighting or zooms to focus attention.
*   **Music:** Upbeat, modern, unobtrusive tech-style music.
*   **Voiceover:** Energetic, clear, and concise.
*   **Length:** Aim for strictly 60 seconds or slightly less.}